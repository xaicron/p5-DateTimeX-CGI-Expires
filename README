This is Perl module DateTimeX::CGI::Expires.

INSTALLATION

DateTimeX::CGI::Expires installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan DateTimeX::CGI::Expires

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

DateTimeX::CGI::Expires documentation is available as in POD. So you can do:

    % perldoc DateTimeX::CGI::Expires

to read the documentation online with your favorite pager.

Yuji Shimada
